{
    "title": "Retrieve a HTLC transaction",
    "description": "Specific HTLC transactions can be obtained either by their `lockId`.",
    "method": "GET",
    "path": "locks/{id}",
    "parameters": {
        "id": {
            "type": "path",
            "description": "The lockId of the HTLC to be retrieved.",
            "rules": ["required", "string"]
        }
    }
}
